[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck — avoid false restarts while debugging port binding.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
